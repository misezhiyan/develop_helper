package ${proj.serviceDir};

import ${proj.entityDir}.${table.className};
import ${proj.responseDir}.${table.className}AddResponse;
import ${proj.responseDir}.${table.className}ListResponse;
import ${proj.responseDir}.${table.className}DelResponse;

/**
 * @discription ${table.TABLE_COMMENT}
 * @author ${proj.author}
 * @date ${date}
 */
public interface ${table.className}Service {

    ${table.className}AddResponse add(${table.className} ${table.className.substring(0,1).toLowerCase()}${table.className.substring(1)});

    ${table.className}ListResponse list(${table.className} ${table.className.substring(0,1).toLowerCase()}${table.className.substring(1)});

    ${table.className}DelResponse del(${table.className} ${table.className.substring(0,1).toLowerCase()}${table.className.substring(1)});
}